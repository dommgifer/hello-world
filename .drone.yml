kind: pipeline
name: default

steps:
- name: build
  image: plugins/docker
  settings:
    USERNAME:
      from_secret: docker_username
    PASSWORD:
      from_secret: docker_password
    repo: dommgifer/helloworld
    tags: 
      - test
